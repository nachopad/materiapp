import { Route } from 'react-router';

export const UserRoutes = () => {
    return (
        <>
            <Route path="/" element={<>Dashboard</>} />
            {/* Otras rutas */}
        </>
    );
};
