# SDD Configuration — materiapp
# Generated by sdd-init phase
# Mode: hybrid (OpenSpec + Engram)
# Date: 2026-05-20

project:
  name: materiapp
  type: fullstack
  root: C:\Users\maxi1\OneDrive\Documentos\dev\materiapp

execution:
  mode: auto
  chained_pr_strategy: ask-always
  review_budget_lines: 400

persistence:
  mode: both
  engram_topic_key: sdd-init/materiapp
  openspec_path: openspec/

stack:
  client:
    framework: React 19 + Vite
    language: TypeScript 5.8
    routing: React Router 7
    state: Zustand 5 with persist
    forms: React Hook Form + Zod 4
    styling: Tailwind CSS v4 + shadcn/ui
    data_fetching: TanStack Query
    http_client: Axios
    icons: Lucide React
    test_runner: Vitest
    test_environment: jsdom
    linter: ESLint 9
    
  server:
    framework: NestJS 11
    language: TypeScript 5.7
    database: MongoDB (Mongoose 8)
    auth: JWT + Google OIDC
    validation: class-validator + Zod
    documentation: Swagger
    test_runner: Jest 30
    test_environment: node
    linter: ESLint 9
    formatter: Prettier 3

testing:
  strict_tdd: true
  coverage_enabled: true
  layers:
    - unit
    - integration
    - e2e (server only)

skill_registry:
  path: .atl/skill-registry.md
  last_updated: 2026-05-20
  
conventions:
  client_agents: client/AGENTS.md
  server_agents: server/AGENTS.md
  root_agents: AGENTS.md
