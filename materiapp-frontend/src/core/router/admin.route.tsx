import { Route } from 'react-router';

export const AdminRoutes = () => {
    return (
        <>
            <Route path="/admin" element={<>Admin</>} />
            {/* otras rutas */}
        </>
    );
};
